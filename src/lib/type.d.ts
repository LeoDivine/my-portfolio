interface IELLIPSE {
  className?: string;
}
interface IMAXWIDTH {
  children: React.ReactNode;
  className?: string;
}

interface INAVLINKS {
  linkname: string;
  link: string;
}

interface ISOCIALS{
  link: string;
  icon: React.ReactNode;
}

interface INEXTSECTION {
  link: string;
  className?: string;
}

interface ISKILLS {
  name: string;
}

interface IPROJECTS {
  image: string;
  title: string;
  content: string;
  link: string;
}